# lanzou-direct-link —— 依赖 / dependencies
#
# === 核心库 + 命令行（必需）/ core library + CLI (required) ===
requests>=2.25

# === 图形界面（可选）/ GUI (optional) ===
# lanzou_gui.py 需要 PyQt6：
#     pip install PyQt6
PyQt6>=6.5

# === 无头浏览器兜底（可选）/ headless browser fallback (optional) ===
# 仅 cli.py --browser 需要；装完还要执行一次 playwright install chromium
# （系统里已有 Edge/Chrome 时可跳过那一步，代码会自动用 channel=msedge/chrome）
#
# playwright>=1.30
